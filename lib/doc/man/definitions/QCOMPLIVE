*Defined in /sys/composite.h

QCOMPLIVE		VBFC composite for previous_object(). It uses the same
			array as the last COMPOSITE command. Good to use in
			say() messages since perhaps other people don't know
			the objects with the same name as you do, maybe they
			can't see invis objects?

