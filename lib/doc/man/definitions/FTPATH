*Defined in /sys/filepath.h

FTPATH(curr_path, given_path)	Popular macro used with load, cat commands and
				all other alike. curr_path is the current path 
				of the wizard, and given_path is your argument.
				It returns the resolved path.
