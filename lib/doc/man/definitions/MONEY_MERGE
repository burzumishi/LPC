Defined in : #include <macros.h>

Definition : (int) MONEY_MERGE( int * )

Description: Merges an array of coins into its value in copper coins.

Arguments  : int * - the money array (smallest denomination first)

Example    : MONEY_MERGE( ({ 1, 2, 3, 4 }) )   ->  7369

