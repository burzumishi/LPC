Definition   PLAYER_I_AUTOLOAD_TIME

Defined in   #include <stdproperties.h>

Description  The 'time' at which the player will be able to quit again. This
             property is only set during login to prevent loss of inventory
             due to eval-cost or other problems. When the autoload is complete,
             this property is removed again.

Arguments    int - the time when the player can quit again.
