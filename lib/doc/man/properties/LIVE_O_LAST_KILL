LIVE_O_LAST_KILL                The object we last commanded to kill. Used by
                                /cmd/std/misc_cmd.c for kill confirmation
                                of livings we have met status with.

