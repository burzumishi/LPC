NAME
	$ - do not parse the command with the quicktyper

SYNOPSIS
	$<command> [<arguments>]

DESCRIPTION
	Prefixing a command with a dollar $ sign will execute that command
	without parsing it through the quicktyper.

SEE ALSO
	quicktyper
